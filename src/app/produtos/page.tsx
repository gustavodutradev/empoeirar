import { PagePlaceholder } from "@/components/site/page-placeholder";

export const metadata = { title: "Produtos" };

export default function Page() {
  return <PagePlaceholder title="Produtos" />;
}
